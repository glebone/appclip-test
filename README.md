# App Clip test target

Static pages used to test Apple App Clip invocation from a development device (Local Experience) and from Apple Messages for Business.

Each page carries an `apple-itunes-app` meta tag with an `app-clip-bundle-id`. No tracking, no data collection, no affiliation with any brand.
